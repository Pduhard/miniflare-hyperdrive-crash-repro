{
  "name": "miniflare-hyperdrive-proxy-crash-repro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The Hyperdrive local proxy pipes two sockets without an error handler, so a socket error takes down the whole Node process.",
  "scripts": {
    "repro:half-close": "node repro-half-close.mjs",
    "repro:reset": "node repro-reset.mjs",
    "fix": "node apply-fix.mjs",
    "unfix": "node apply-fix.mjs --revert"
  },
  "devDependencies": {
    "miniflare": "4.20260730.0"
  }
}
